pipeline {
	agent any
		stages {
			stage(“Checkout”) {
				steps {
					git url: ‘’
				}
			}
			stage(“Compile”) {
				steps {
					sh "./gradlew compileJava"
				}
			}
			stage(“Unit test”) {
				steps {
					sh "./gradlew test"
				}
			}
		}
	}
